# ReconAgentPcap Dependencies

# Core agent framework
agents>=0.1.0
openai>=1.0.0

# MCP server framework
mcp>=0.1.0

# PCAP analysis requires tshark (Wireshark command-line tool)
# Install with:
#   macOS: brew install wireshark
#   Linux: sudo apt install tshark
#   Windows: Download from https://www.wireshark.org/download.html

# Note: These should already be installed if you've set up the defense environment
# Install with: pip install -r requirements.txt
