<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Panel</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body class="page">
  <div class="glow" aria-hidden="true"></div>
  <main>
    <section class="card glass">
      <span class="badge">Admin · Sofia Ops</span>
      <h2>Operations dashboard</h2>
      <p class="muted">This interface is temporary. No password configured yet. Please don't tell compliance.</p>

      <div class="details">
        <div><strong>Database file</strong><span><%= dbPath %></span></div>
      </div>

      <form action="/download-db" method="GET" class="form inline">
        <label>File name
          <input type="text" name="file" value="users.db" />
        </label>
        <button class="btn primary" type="submit">Download DB</button>
      </form>

      <div class="muted" style="margin-top: 1rem;">
        Need backups? Try <a href="/backup-db">/backup-db</a> or peek <a href="/config-prod">config</a>.
      </div>
    </section>

    <section class="table-card">
      <h3>Client roster</h3>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Password</th>
            <th>Card last4</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(user => { %>
            <tr>
              <td><%= user.name %></td>
              <td><%= user.email %></td>
              <td><%= user.phone %></td>
              <td><%= user.password %></td>
              <td><%= user.credit_card_last4 %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </section>
  </main>
</body>
</html>
